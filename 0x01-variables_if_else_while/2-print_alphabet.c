#include<stdio.h>

/**
 * main-printsthe alphabet iin lowercase
 * followed by anew line
 * Return:Always 0 (Success)
 */
int main(void)
{
	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
