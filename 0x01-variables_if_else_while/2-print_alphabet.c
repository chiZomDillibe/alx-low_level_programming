#include<stdio.h>

/**
 * main-printsthe alphabet iin lowercase
 * followed by anew line
 * Return:Always 0 (Success)
 */
int main(void)
{
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
